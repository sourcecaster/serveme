name: serveme
description: Framework for console server applications
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  mongo_dart: ^0.7.0+2
  yaml: ^3.1.0
  connectme:
    git: https://github.com/MadSheogorath/connectme.git
  packme:
    git: https://github.com/MadSheogorath/packme.git
